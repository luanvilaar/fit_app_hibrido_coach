# Story 1.33 — Programas estruturados e entrega personalizada

Status: Ready for Review

## Executor Assignment

executor: "@dev"
quality_gate: "@aiox-qa"
security_review: "@aiox-data-engineer + @aiox-qa"
quality_gate_tools: ["npm run lint", "npm run typecheck", "npm test", "npm run build"]

## Story

**As a** coach FitBlock,
**I want** montar um programa com várias semanas e sessões a partir da minha biblioteca,
**so that** eu possa vender uma versão fechada na Loja e entregar cópias personalizadas para atletas ou equipes.

## Critérios de aceitação

1. Um produto de treino permite organizar várias sessões em semanas e dias, incluindo dias de descanso,
   sem limitar o programa a `S1 · D1`.
2. O coach só consegue selecionar templates de sessão que pertencem a ele, ou ao owner quando autorizado;
   IDs enviados pelo cliente não concedem ownership.
3. Enviar um produto para revisão valida conteúdo comercial e estrutura mínima do programa, incluindo
   título, resumo, preço válido e ao menos uma sessão vinculada.
4. O admin consegue revisar os metadados e a estrutura de sessões antes de publicar; qualquer edição de
   produto em revisão retorna o produto para rascunho e exige nova submissão.
5. A publicação cria uma versão imutável do conteúdo das sessões. Alterações posteriores na biblioteca
   não modificam programas já publicados ou comprados.
6. O catálogo público expõe somente dados comerciais e títulos das sessões, nunca snapshots completos,
   IDs internos de templates ou conteúdo pago para usuários sem acesso.
7. O atleta que compra um programa recebe acesso à versão publicada e consegue visualizar suas semanas,
   abrir uma sessão liberada e iniciar o fluxo existente de execução sem receber acesso a produto de outro
   atleta.
8. Um coach pode criar uma entrega personalizada para uma equipe ou atleta autorizado a partir de uma
   versão publicada, sem alterar o produto comercial original.
9. Leituras e mutações de produtos, versões, entregas e sessões respeitam autenticação, papel, ownership,
   vínculo de equipe e acesso comprado no backend/RLS; manipular IDs na URL, no payload ou no DevTools
   não ultrapassa essas regras.
10. Checkout, webhook e concessão de acesso permanecem idempotentes; reentrega de webhook não duplica
    pedido, versão de acesso ou entrega.
11. Migrations possuem snapshot e rollback, e testes cobrem domínio, ownership, RLS/RPC, publicação,
    snapshot, acesso pago, entrega personalizada e ataques negativos.

## Tasks / Subtasks

- [x] Modelar versão imutável de programa e composição semanal/diária.
- [x] Criar migration, snapshot e rollback com constraints, índices, RLS e RPCs de leitura/escrita.
- [x] Atualizar publicação/moderação para congelar conteúdo e invalidar revisão após edição.
- [x] Atualizar repositório tipado para múltiplas sessões, versões e entregas.
- [x] Atualizar editor do coach para montar semanas, dias de descanso e selecionar templates próprios.
- [x] Atualizar detalhe/Meus Treinos para navegar por sessões liberadas e iniciar execução.
- [x] Adicionar fluxo de entrega personalizada para equipe/atleta autorizado.
- [x] Adicionar testes funcionais, de contrato, ownership, acesso pago e segurança negativa.
- [x] Executar revisão independente QA e segurança; corrigir todos os achados críticos/altos.
- [x] Executar gates, atualizar File List, Completion Notes e Change Log.
- [x] Consolidar duração obrigatória em semanas, matriz relativa completa (W×7), cinco tipos de dia e metadados comerciais imutáveis na versão publicada.
- [x] Persistir a data inicial escolhida pelo aluno antes do webhook e expor as datas calculadas somente na instância do aluno.
- [x] Corrigir a auditoria QA #1: congelar a versão no pedido PIX, normalizar legado W×7 e preservar acesso/entrega na recompra.
- [x] Integrar a agenda real do atleta aos cinco tipos de dia e ampliar a revisão do owner com todos os metadados comerciais e grade.

## Dev Notes

### Fonte de requisitos

- Aprovação do usuário para implementar as opções 1 e 3: programa fechado com compra única e entrega
  personalizada derivada de uma versão publicada.
- `SPEC-PAGAMENTOS-MARKETPLACE-FITBLOCK.md` §§18–32: produto, acesso, moderação, permissões e segurança.
- `docs/stories/1.31.training-marketplace-mvp.md`: catálogo, checkout PIX, webhook e liberação idempotente.
- `docs/stories/1.32.security-hardening-pentest.md`: padrões de RLS, RPCs protegidas e testes negativos.

### Decisões

- A Loja continua sendo a única vitrine.
- A biblioteca de sessões continua sendo reutilizada como fonte de criação, mas a publicação deve gerar
  snapshot imutável para impedir alteração retroativa de conteúdo vendido.
- Assinaturas, split/comissão, reembolso e produtos físicos ficam fora desta story.
- A entrega personalizada é uma cópia/instância derivada da versão publicada; nunca edita o produto ou
  a versão comercial original.
- Conteúdo completo e IDs internos de templates só saem por RPCs autorizadas para o coach proprietário,
  owner, comprador ou destinatário da entrega, conforme o caso.

### Testes esperados

- Domínio: ordenação semanal, dias de descanso, validação de programa e estados de publicação.
- Backend/RLS: cross-tenant, BFLA, enumeração de IDs, acesso sem compra, produto arquivado e edição em revisão.
- Pagamentos: preço relido no servidor, webhook adulterado/repetido e concessão única.
- UI: editor multi-semana, revisão, catálogo, Meus Treinos e entrega personalizada.
- Quality gates: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` e `git diff --check`.

## Dev Agent Record

### Agent Model Used

GPT-5 Codex — Dev/Architect, QA Supervisor e Security/Data Reviewer

### Completion Notes List

- Editor permite agenda multi-semana, dias 1–7, descansos, repetição segura de templates publicados e revisão visual da estrutura pelo owner.
- Publicação cria snapshot versionado imutável; compradores e entregas executam instâncias derivadas do snapshot, não da biblioteca viva.
- Entrega para equipe é compartilhada; entrega individual cria equipe privada e valida que o atleta pertence a uma equipe autorizada do coach.
- Webhook/settlement mantém preço validado no servidor, acesso versionado e idempotência para não duplicar entrega ou sessões.
- QA Supervisor: PASS após corrigir tratamento assíncrono de edição, progressão do 7º para o 1º dia da semana e controles acessíveis.
- Security/Data Reviewer: PASS local; ownership, role, IDs, RLS, snapshots e acesso cruzado foram revisados. Nenhum achado crítico/alto permanece.
- Gates locais: `npm run lint` PASS; `npm run typecheck` PASS; `npm test -- --runInBand --silent` PASS (60 suítes / 510 testes); `npm run build` PASS; `git diff --check` PASS.
- `npx supabase db lint --local` ficou bloqueado porque não há Postgres/Docker local ativo (`127.0.0.1:54322`); executar no ambiente Supabase de staging.
- O teste da home recebeu apenas mock de `expo-video` para ambiente Jest; não altera runtime.
- Migration não foi aplicada ao Supabase nesta etapa. Antes de produção, aplicar em staging e executar testes reais de RLS/cross-tenant, compra PIX, reentrega de webhook e rollback autorizado.
- Revisão de implementação 2026-08-17: duração passou a ser obrigatória e gera exatamente 7 dias relativos por semana. Cada posição persiste `training`, `rest`, `recovery`, `assessment` ou `unprogrammed`; apenas `training` recebe template/snapshot/instância executável.
- O editor permite reutilizar templates publicados e abrir o mesmo `SessionComposer` da Agenda para montar um treino novo diretamente em um dia do programa.
- A data inicial é escolhida no checkout, validada e persistida em `store_orders.program_start_date`; o settlement relê apenas esse valor e cria datas reais em `training_program_delivery_sessions.scheduled_date`.
- A versão publicada agora congela metadados comerciais e estrutura. Gates desta revisão: lint PASS, typecheck PASS, test PASS (64 suítes / 542 testes), build PASS e `git diff --check` PASS. `npx supabase db lint --local` permanece bloqueado sem Postgres/Docker em `127.0.0.1:54322`.
- Correção QA #1 (2026-08-17): o checkout consulta e grava `store_orders.program_version_id` antes de abrir o PIX. O settlement aceita apenas essa versão, valida que ela pertence ao item comprado e não troca `version_id`, `order_id` nem a entrega já existente em retry/recompra.
- A migration completa snapshots e entregas históricas para `duration_weeks × 7`, preservando snapshots de treino anteriores e preenchendo lacunas somente como `unprogrammed` com datas derivadas da entrega.
- O calendário real do atleta passou a usar `list_athlete_calendar_entries`: sessões de treino seguem executáveis e os quatro demais tipos aparecem datados, selecionáveis para leitura e sem ação de iniciar.
- O owner visualiza descrição, categoria, objetivo, nível, duração, preço e a estrutura protegida antes de aprovar. A vitrine/detalhe público também exibe objetivo.
- Gates finais desta correção: `npm run lint` PASS; `npm run typecheck` PASS; `npm test -- --runInBand --silent` PASS (66 suítes / 552 testes); `npm run build` PASS; `git diff --check` PASS.

### Debug Log References

- `npx supabase db lint --local` bloqueado pelo ambiente: `LegacyDbConnectError` ao conectar em `127.0.0.1:54322` (Docker/Postgres local indisponível). Não foi interpretado como aprovação de migration.
- `npm run verify:schema` não pôde consultar o ambiente remoto (`fetch failed`); a migration continua pendente de aplicação e validação real em staging com RLS/checkout/webhook.

### File List

- `apps/universal/components/coach/coach-products-screen.tsx`
- `apps/universal/components/coach/coach-products-screen.test.tsx`
- `apps/universal/components/calendar-screen.tsx`
- `apps/universal/components/calendar-screen.test.tsx`
- `apps/universal/components/coach-hibrido/week-board.tsx`
- `apps/universal/components/my-training-programs-screen.tsx`
- `apps/universal/components/my-training-programs-screen.test.tsx`
- `apps/universal/components/store-screen.tsx`
- `apps/universal/components/public-home.test.tsx` (mock de teste nativo preexistente)
- `apps/universal/data/program-builder.ts`
- `apps/universal/data/program-builder.test.ts`
- `apps/universal/data/calendar.ts`
- `apps/universal/data/calendar-backend.test.ts`
- `apps/universal/data/store-backend.test.ts`
- `apps/universal/data/store.test.ts`
- `apps/universal/data/training-program-migration.test.ts`
- `api/store/checkout.ts`
- `api/store/checkout.test.ts`
- `packages/backend/src/store-repository.ts`
- `packages/backend/src/calendar-repository.ts`
- `supabase/migrations/20260819100000_training_program_versions_delivery.sql`
- `supabase/rollback/20260819100000_training_program_versions_delivery.sql`
- `supabase/snapshots/20260819100000_pre_training_program_versions_delivery.md`
- `docs/stories/1.33.training-program-builder-delivery.md`

### Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-14 | 1.0 | Story criada a partir da aprovação das opções 1 e 3, com revisão QA e segurança prevista. | Codex |
| 2026-08-14 | 1.1 | Implementação local concluída, revisada por QA/segurança e preparada para validação em staging. | Codex |
| 2026-08-17 | 1.2 | Calendário relativo W×7, cinco tipos de dia, SessionComposer reutilizado, objetivo comercial, snapshot de metadados e data inicial persistida no checkout. | Codex |
| 2026-08-17 | 1.3 | Correções QA #1: versão congelada no PIX/settlement, normalização histórica W×7, agenda datada do atleta e revisão comercial completa do owner. | Codex |
